import { CategoryLabelsPipe } from './category-labels-pipe';

describe('CategoryLabelsPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryLabelsPipe();
    expect(pipe).toBeTruthy();
  });
});
